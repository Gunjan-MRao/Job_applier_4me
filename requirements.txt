# =============================================================================
# Job Application Copilot — SINGLE SOURCE OF TRUTH for all dependencies
# Install with:  pip install -r requirements.txt
# =============================================================================

# ── Core API ──────────────────────────────────────────────────────────────────
fastapi~=0.111.0
uvicorn[standard]~=0.29.0
pydantic[email]~=2.7.0
pydantic-settings~=2.2.0
email-validator~=2.1.0
python-multipart~=0.0.9
aiofiles~=23.2.0
httpx~=0.27.0
python-dotenv~=1.0.0

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy~=2.0.0
alembic~=1.13.0

# ── Resume parsing ────────────────────────────────────────────────────────────
pypdf~=4.2.0
python-docx~=1.1.0

# ── Job scraping ──────────────────────────────────────────────────────────────
# Note: python-jobspy renumbered from 0.1.x → 1.1.x; 0.1.24 does not exist
python-jobspy~=1.1.0
beautifulsoup4~=4.12.0
# requests 2.32.x supports urllib3>=2 and charset-normalizer>=3 — silences
# the RequestsDependencyWarning seen with requests~=2.31.0 + newer transitive deps
requests>=2.32.0,<3.0
urllib3>=2.0.0,<3.0
charset-normalizer>=3.0.0,<4.0
lxml~=5.2.0

# ── Stealth browser (Playwright — primary anti-Cloudflare path) ───────────────
# After pip install run:  playwright install chromium
playwright~=1.44.0
playwright-stealth~=1.0.6
# undetected-chromedriver is the Selenium fallback used when Playwright
# is unavailable.  It is imported with try/except so the app still runs
# if it is not installed.
# undetected-chromedriver~=3.5.5

# ── AI providers ──────────────────────────────────────────────────────────────
# Groq is free and the primary LLM (Phase 1)
groq>=0.9.0
# Gemini is free fallback — no extra package needed (uses requests).
# Uncomment the paid providers you want to use:
# openai~=1.30.0
# anthropic~=0.28.0

# ── Config ────────────────────────────────────────────────────────────────────
pyyaml~=6.0.0

# ── Lead finder ───────────────────────────────────────────────────────────────
# Hunter.io and Apollo.io are called via plain requests — no SDK needed.
# Adzuna is also plain requests.

# ── Data / export ─────────────────────────────────────────────────────────────
reportlab~=4.2.0
openpyxl~=3.1.0
pandas~=2.2.0

# ── Frontend ──────────────────────────────────────────────────────────────────
streamlit~=1.35.0

# ── Dev / test ────────────────────────────────────────────────────────────────
pytest~=8.2.0
pytest-asyncio~=0.23.0
httpx~=0.27.0
