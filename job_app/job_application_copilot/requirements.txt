# This file is kept for backwards compatibility.
# The canonical dependency list is at the repo root: /requirements.txt
#
# Install from the repo root:
#   pip install -r requirements.txt
#
# Or if running this sub-package in isolation, install the root file:
#   pip install -r ../../requirements.txt
